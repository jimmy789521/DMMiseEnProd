spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/toeic-db
    username: postgres
    password: admin
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
  liquibase:
    change-log: db/changelog/db.changelog-master.yaml
